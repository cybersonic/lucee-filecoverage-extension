component extends="testbox.system.BaseSpec" {
	function run(){
		describe( "Basic Sanity Checks", function() {

			it( "the driver should compile", function() {

				// var data = getComponentMetadata("src.context.admin.debug.FileCoverage");
				
			} );
			
		} );
	}
}